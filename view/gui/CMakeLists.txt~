set(project_name "view_gui")

file(GLOB HEADERS "*.h")
file(GLOB SOURCES "*.cpp")
file(GLOB FORMS "forms/*.ui")

#qt5_wrap_ui(FORM_HEADERS ${FORMS} )
wrap_qt_ui_files(FORM_HEADERS ${project_name} ${FORMS})

# Subdirectories
add_subdirectory(canvas)

add_executable(${project_name} ${SOURCES} ${HEADERS} ${FORM_HEADERS})

target_link_libraries(${project_name} Qt5::Core Qt5::Widgets core)

# generate ts files
generateTranslations()
