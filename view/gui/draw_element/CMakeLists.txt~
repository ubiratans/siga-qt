set(project_name "view_draw_element")

file(GLOB HEADERS *.h)
file(GLOB SOURCES *.cpp)

add_library(${project_name} ${SOURCES} ${HEADERS})

target_link_libraries(${project_name} Qt5::Core Qt5::Widgets core)
