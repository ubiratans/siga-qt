set(project_name "view_canvas_draw_element")

file(GLOB HEADERS *.h)
file(GLOB SOURCES *.cpp)

add_executable(${project_name} ${SOURCES} ${HEADERS})

target_link_libraries(${project_name} Qt5::Core core)
